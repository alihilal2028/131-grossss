[build]
  base = "frontend"
  command = "yarn install && yarn build"
  publish = "build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
  CI = "false"
  DISABLE_ESLINT_PLUGIN = "true"
